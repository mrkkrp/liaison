-- errors, etc. test combinators that we provide I think similarly to what
-- we have in forma, but I want to cover all cases per every combinator

module Liaison.ValidationSpec
  ( spec )
where

import Test.Hspec

spec :: Spec
spec = return ()
